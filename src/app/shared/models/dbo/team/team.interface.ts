export interface TeamModel {
    teamName: string;
    // points: number;
    // playedMatches: number;
    // wonMatches: number;
    // drawnMatches: number;
    // lostMatches: number;
    // goalsFor: number;
    // goalsAgainst: number;
}